{% if patternlab %}
  {% if image.src %}
    <img src="{{- image.src -}}" class="{{ image.classes|join(' ')|trim }}"{% if image.alt %} alt="{{ image.alt }}"{% endif %} />
  {% endif %}
{% else %}
  {{ image }}
{% endif %}
